package etc.orderEx2;

public class OrderService {
    public void processOrder(int money, int amount) {
        System.out.println("주문을 시작합니다: 보유 금액 = " + money + ", 배고픔 정도 = " + amount);

//        Guest guest = GuestStore.findGuest(money);
//        boolean result = guest.guest(money, amount);
    }
}
